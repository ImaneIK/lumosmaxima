import 'package:flutter/material.dart';
import 'package:test1/pages/user_page.dart';

class HeaderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        UserPage(),
      ],
    );
  }
}
